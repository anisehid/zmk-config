# Diagnostic shield: scans every anise_ctl pin as a matrix row against the
# known-good columns, to locate which pin the top rows are actually wired to.
CONFIG_ZMK_USB_LOGGING=y
CONFIG_ZMK_LOG_LEVEL_DBG=y
CONFIG_NFCT_PINS_AS_GPIOS=y
CONFIG_ZMK_RGB_UNDERGLOW=n
CONFIG_ZMK_BLE=n
