# Migration in progress: only the anisectlc + anise60b pair is on the new
# upstream-ZMK / hardware-model-v2 path. The rest are added back once this
# one is verified on real hardware. v1.0.0 remains the working release.
---
include:
  - board: anisectlc/nrf52840/zmk
    shield: anise60b_left
  - board: anisectlc/nrf52840/zmk
    shield: anise60b_right
